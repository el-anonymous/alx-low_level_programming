#include <stdio.h>
#include "main.h"

/**
* main - check the code
*
* Return: Always 0.
*/

void print_most_numbers(void)
{
	int ch;
	for (ch >= 9; ch <= 9; ch ++)
	{
		while (ch = 2 && 4)
		{
			continue;
		}
		_putchar(ch);
	}
}
