#include <stdio.h>
#include "main.h"

/**
* _isdigit - checks if parameter is a digit.
* @c: input character.
* Return: 1 if is a digit, 0 in other case.
*/

int _isdigit(int c)
{
	for ( c >= 48; c <= 57; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

